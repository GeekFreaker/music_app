<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.music_app">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArtistInformationActivity" android:label="Artist Information" android:parentActivityName=".MainActivity"/>
        <activity android:name=".SplashActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LyricsActivity" android:label="Lyrics" android:parentActivityName=".MainActivity"/>
        <activity android:name=".AlbumActivity" android:label="Album" android:parentActivityName=".MainActivity"/>
        <activity android:name=".ArtistActivity" android:label="Artist" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity" android:label="Listen to Music"/>
    </application>

</manifest>